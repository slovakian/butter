approach 2 makes the most sense. so, we want to implement an oRPC procedure used to interact with board related items in the database. this seems complex, because if someone wants to fetch a board that is private, we might only want to fetch certain details about it like its name and slug and maybe statistics, but when the page loads, we only show a stripped down version of the board with a dialog saying you need to be a member of the board.

if the board is read-only, we might want to let someone fetch the board, but the procedures used to interact are cut off from the non-member.

Can we implement an all-emcompassing orpc procedure to handle these conditional cases? I think it's possible.. 


we want a procedure to use to protect endpoints depending both on the status of a board and whether a user is a member and what their role is. 

So, orpc has auth.api.hasPermission({ headers, body: { organizationId: 
"", permissions: checkAnyPermissionsObject }}) to let us check is a user has permissions. but we also have this "status" column on the row "public", "private", or "read_only"... or maybe even "users_only" so that random non-users can't view a board without being logged in

orpc let's us write middleware that extract inputs from the .input() on a procedure

Middleware Input
Middleware can access input, enabling use cases like permission checks.


const canUpdate = os.middleware(async ({ context, next }, input: number) => {
  // Perform permission check
  return next()
})

const ping = os
  .input(z.number())
  .use(canUpdate)
  .handler(async ({ input }) => {
    // Handler logic
  })

// Mapping input if necessary
const pong = os
  .input(z.object({ id: z.number() }))
  .use(canUpdate, input => input.id)
  .handler(async ({ input }) => {
    // Handler logic
  })
INFO

You can adapt a middleware to accept a different input shape by using .mapInput.


const canUpdate = os.middleware(async ({ context, next }, input: number) => {
  return next()
})

// Transform middleware to accept a new input shape
const mappedCanUpdate = canUpdate.mapInput((input: { id: number }) => input.id)

// here is an example of how to define a middleware

    export const someMiddleware = (opts: SomeOpts) =>
	authMiddleware.concat( // we use auth middleware so IF a user is auth'd, the middleware can access it... keep in mind that some things related to a board can be accessed by non-logged in people as well
		async (
			{ context, next },
			input: { boardId: string }, // some value we extract from input
		) => {
			// some logic...
			return next({ // we can modify context with middleware
				context: {
					auth: context.auth,
				},
			});
		},
	);


and we can also define a procedure with a wrapper so we can pass information to the middleware which is known at the time of procedure definition like boardProcedure(args).input(z.object({ boardId: z.string() // extracted by middleware somehow })).handler(async({context, input }) => { // ... // we can access context.auth.session // or context.auth.client.api and so on})



And so we need to think about an intelligent solution to control access to boards depending on several things: - board status ("public", "private", "read_only") - a permissions object passed to the procedure which gets forwarded to the api.hasPermission method under the right circumstance...



so, we need to think deeply about a solution that easily handles all these scenarios. one procedure to rule them all!



api.board.message.create() can use this procedure. api.board.page.get() can use it, api.board.admin.user.ban() can use it, etc.



It must be super flexible. so, based on the variables i gave you and all this information, let's focus on how we can implement this!

please keep in mind what the different board member roles mean:
- private: only members can use the board
- public: anyone can use the board
- unlisted/user_only: only auth'd users can use the board
- read_only: only members can use the board, but nonmembers can view it

"use" here is being used broadly. 

It might also help to show you the permissions ac statement. maybe we need to revise it too. not sure.

import { createAccessControl } from "better-auth/plugins/access";

export type BoardPermissions = typeof boardStatement;

const boardStatement = {
	board: ["update", "delete"],
	member: ["create", "update", "delete", "ban"],
	message: ["create", "update", "delete"],
} as const;

const boardAc = createAccessControl(boardStatement);

export const member = boardAc.newRole({});

export const admin = boardAc.newRole({});

export const owner = boardAc.newRole({});


As you can see, i haven't fill out the roles for each role type yet because i dont know how we're implementing this....

It's possible that we come up with a creative solution. ultimately, we are always checking permissions in some sense, but we aren't always using .hasPermission. for example, let's say we want to let non-members create messages on the board. we might do something like....

const createMessage = boardAdminProcedure({
	strict: true, // this will throw
    permissions: {
        message: ["create"]
    }
})
	.input(z.object({ boardId: z.string() }))
	.handler(async ({ context, input }) => {
        // ... logic to create a board message
    });

const loadBoardChannelWithMessage = boardAdminProcedure({
	strict: true, // this will throw
    permissions: {
        message: ["read"]
    }
})
	.input(z.object({ boardId: z.string() }))
	.handler(async ({ context, input }) => {
        // ... logic to load a board channel with messages
    });

// or let's say we DONT want to throw

const loadBoard = boardAdminProcedure({
	strict: false, // this will not throw
    permissions: {
        board: ["read"]
    }
})
	.input(z.object({ boardId: z.string() }))
	.handler(async ({ context, input }) => {
        // ... logic to load a board
        context.board.memberRole // we can now access the member role
        context.board.hasPermission // we can access whether they have permission.. a public board won't even check this permission, it'll just return true here.
        context.board.data.status // we can access board status
    });

(keep in mind i am not glued to any specific implementation, this is JUST AN IDEA!!!)

Basically, we need some way to dynamically bypass or apply certain permissions checks based on the variables we described. 
so, i think this is the major concern i have is that we want to dynamically apply permissions checks depending on the nature of the procedure. 
so, we know we have an ac statement of the total possible resources and corresponding actions we CAN check. 
we know the user's auth status: whether is is no user, whether there is a user, and whether or not someone is 
a member of the board (by fetching the board in the middle and including members where we filter for the current user if there is one)... 
and we know what permissions the procedure is expecting.. 
so now we need some way of applying a condition matrix to conditionally apply permissions checks and if a check fails, 
we conditionally throw depending on "strict" in the procedure args. 
this is all just idea!!! please let me know what you think....